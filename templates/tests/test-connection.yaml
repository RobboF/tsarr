apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsarr.fullname" . }}-test-connection"
  labels:
    {{- include "tsarr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tsarr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
